{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/",
      "dest": "/index.html"
    },
    {
      "src": "/(?!.*\\.(js|css|png|jpg|jpeg|svg|gif|ico|json|webp)$)(.*)",
      "dest": "/index.html"
    }
  ]
}
